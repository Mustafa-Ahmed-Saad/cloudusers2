const HomePage = () => (
  <div className="home">
    <div className="container">
      <div className="overlay">
        <h2>Home page</h2>
        <div>
          this app created using react, you can get, add, edit and delet any user you want, And all this is done by http ajax request / api (mock api). if you add or edit user application will send http ajax request to api and If there is any problem or error, it will appear as alert and if you want
          to see requests (get, delete, put, post) when get or edit or delet or add user you can open inspect then network
        </div>
      </div>
    </div>
  </div>
);

export default HomePage;
